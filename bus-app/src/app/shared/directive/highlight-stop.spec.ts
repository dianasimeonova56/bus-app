import { HighlightStop } from '../highlight-stop';

describe('HighlightStop', () => {
  it('should create an instance', () => {
    const directive = new HighlightStop();
    expect(directive).toBeTruthy();
  });
});
